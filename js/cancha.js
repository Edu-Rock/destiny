const canchaManager={
  init(){this.populateSelect();},
  populateSelect(){const equipos=Storage.getEquipos(),s=document.getElementById("select-equipo-cancha");s.innerHTML=equipos.length?equipos.map(e=>`<option value="${e.id}">${e.nombre}</option>`).join(""):`<option value="">No hay equipos inscritos</option>`;if(equipos[0])this.cargarFormacion(equipos[0].id);else document.getElementById("pitch-grid").innerHTML=`<div class="empty-state" style="color:#fff">Inscribe un equipo para visualizar la formación.</div>`;},
  cargarFormacion(id){const e=Storage.getEquipos().find(x=>x.id===id);if(!e)return;const groups=[e.jugadores.filter(j=>j.posicion==="Delantero"),e.jugadores.filter(j=>j.posicion==="Mediocampista"),e.jugadores.filter(j=>j.posicion==="Defensa"),e.jugadores.filter(j=>j.posicion==="Arquero")];const grid=document.getElementById("pitch-grid");grid.innerHTML=groups.map(gs=>`<div class="pitch-row">${gs.map(j=>`<div class="player-card-token" title="${j.nombre} · ${j.posicion}"><div class="jersey-icon" style="--kit:${e.color1 || "#16a34a"}"><div class="jersey-body"></div><div class="jersey-number">${j.numero || ""}</div></div><div class="player-name-tag">${j.nombre}</div></div>`).join("")}</div>`).join("");}
};